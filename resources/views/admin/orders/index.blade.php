@extends('layouts.admin')

@section('content')
<div style="display:grid; gap:20px;">
    <div style="display:flex; justify-content:space-between; align-items:center; gap:16px; flex-wrap:wrap;"><div><span class="eyebrow">Vận hành</span><h1 style="margin:0;">Đơn hàng</h1></div></div>
    <form method="GET" action="{{ route('admin.orders.index') }}" class="filter-box" style="display:flex; gap:12px; flex-wrap:wrap; align-items:end;"><div style="flex:1; min-width:220px;"><label for="q">Tìm kiếm</label><input id="q" name="q" value="{{ request('q') }}" placeholder="Mã đơn, tên hoặc email" style="width:100%; padding:10px;"></div><div><label for="status">Trạng thái đơn</label><select id="status" name="status" style="padding:10px;"><option value="">Tất cả</option>@foreach($orderStatuses as $status)<option value="{{ $status }}" {{ request('status') === $status ? 'selected' : '' }}>{{ strtoupper($status) }}</option>@endforeach</select></div><div><label for="payment_status">Thanh toán</label><select id="payment_status" name="payment_status" style="padding:10px;"><option value="">Tất cả</option>@foreach($paymentStatuses as $status)<option value="{{ $status }}" {{ request('payment_status') === $status ? 'selected' : '' }}>{{ strtoupper($status) }}</option>@endforeach</select></div><button class="btn btn-secondary" type="submit">Lọc</button></form>
    <div class="filter-box" style="overflow-x:auto;"><table style="width:100%; min-width:900px; border-collapse:collapse;"><thead><tr><th style="text-align:left; padding:10px;">Mã đơn</th><th style="text-align:left; padding:10px;">Khách hàng</th><th style="text-align:right; padding:10px;">Tổng tiền</th><th style="text-align:left; padding:10px;">Thanh toán</th><th style="text-align:left; padding:10px;">Trạng thái</th><th style="text-align:left; padding:10px;">Ngày tạo</th><th style="padding:10px;"></th></tr></thead><tbody>@forelse($orders as $order)<tr><td style="padding:10px; border-top:1px solid #e5e7eb; font-weight:600;">{{ $order->order_number }}</td><td style="padding:10px; border-top:1px solid #e5e7eb;">{{ $order->customer_name }}<small style="display:block; color:#6b7280;">{{ $order->customer_email ?? $order->user?->email }}</small></td><td style="padding:10px; border-top:1px solid #e5e7eb; text-align:right;">{{ number_format((float) $order->total_amount, 0, ',', '.') }}đ</td><td style="padding:10px; border-top:1px solid #e5e7eb;">{{ strtoupper($order->payment_status) }}</td><td style="padding:10px; border-top:1px solid #e5e7eb;">{{ strtoupper($order->status) }}</td><td style="padding:10px; border-top:1px solid #e5e7eb;">{{ $order->created_at->format('d/m/Y H:i') }}</td><td style="padding:10px; border-top:1px solid #e5e7eb;"><a class="btn btn-secondary" href="{{ route('admin.orders.show', $order) }}">Chi tiết</a></td></tr>@empty<tr><td colspan="7" style="padding:18px; text-align:center;">Chưa có đơn hàng phù hợp.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}
</div>
@endsection