@extends('layouts.admin')
@section('content')<div class="filter-box" style="max-width:850px"><h1>{{ $contactMessage->subject ?: 'Tin nhắn liên hệ' }}</h1><p><strong>{{ $contactMessage->name }}</strong> · {{ $contactMessage->email }} · {{ $contactMessage->phone }}</p><div style="white-space:pre-wrap;padding:16px;background:#f7f8f9">{{ $contactMessage->message }}</div><form method="POST" action="{{ route('admin.contact-messages.update',$contactMessage) }}" style="margin-top:16px">@csrf @method('PUT')<select name="status">@foreach(['new'=>'Chưa đọc','read'=>'Đã đọc','resolved'=>'Đã xử lý'] as $value=>$label)<option value="{{ $value }}" @selected($contactMessage->status===$value)>{{ $label }}</option>@endforeach</select><button class="btn btn-primary">Cập nhật</button></form></div>@endsection
