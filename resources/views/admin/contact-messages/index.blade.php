@extends('layouts.admin')
@section('content')<div style="display:grid;gap:20px"><h1>Liên hệ khách hàng</h1><form method="GET" class="filter-box" style="display:flex;gap:10px"><input name="q" value="{{ request('q') }}" placeholder="Tên, email, chủ đề"><select name="status"><option value="">Tất cả</option>@foreach(['new'=>'Chưa đọc','read'=>'Đã đọc','resolved'=>'Đã xử lý'] as $value=>$label)<option value="{{ $value }}" @selected(request('status')===$value)>{{ $label }}</option>@endforeach</select><button class="btn btn-secondary">Lọc</button></form><div class="filter-box"><table style="width:100%"><tr><th>Người gửi</th><th>Chủ đề</th><th>Trạng thái</th><th>Ngày</th></tr>@foreach($messages as $message)<tr><td><a href="{{ route('admin.contact-messages.show',$message) }}">{{ $message->name }}</a><br><small>{{ $message->email }}</small></td><td>{{ $message->subject ?? '-' }}</td><td>{{ $message->status }}</td><td>{{ $message->created_at }}</td></tr>@endforeach</table>{{ $messages->links() }}</div></div>@endsection
