@extends('layouts.admin')
@section('content')<div style="display:grid;gap:20px"><h1>Newsletter</h1><form method="GET" class="filter-box" style="display:flex;gap:10px"><input name="q" value="{{ request('q') }}" placeholder="Tìm email"><select name="status"><option value="">Tất cả</option><option value="active">Đang đăng ký</option><option value="inactive">Ngừng đăng ký</option></select><button class="btn btn-secondary">Lọc</button></form><div class="filter-box"><table style="width:100%"><tr><th>Email</th><th>Trạng thái</th><th></th></tr>@foreach($subscribers as $subscriber)<tr><td>{{ $subscriber->email }}</td><td>{{ $subscriber->is_active ? 'Đang đăng ký' : 'Đã ngừng' }}</td><td><form method="POST" action="{{ route('admin.newsletter-subscribers.update',$subscriber) }}">@csrf @method('PUT')<input type="hidden" name="is_active" value="{{ $subscriber->is_active ? 0 : 1 }}"><button class="btn btn-secondary">{{ $subscriber->is_active ? 'Ngừng' : 'Kích hoạt' }}</button></form></td></tr>@endforeach</table>{{ $subscribers->links() }}</div></div>@endsection
